[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
buildTarget = "mcp-http"

[deploy]
startCommand = "python -u -m src.mcp.server"
healthcheckPath = "/health"
healthcheckTimeout = 10
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
