[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
buildTarget = "bot"

[deploy]
startCommand = "alembic upgrade head && echo '--- Migrations done, starting bot ---' && python -u -m src.main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
