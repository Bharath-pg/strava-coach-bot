[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
buildTarget = "bot"

[deploy]
startCommand = "alembic upgrade head && python -m src.main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
